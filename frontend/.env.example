# Copy to .env to override the default.
#
# Left blank, the app uses relative "/api" URLs, which the Vite dev server
# proxies to http://localhost:5000 (see vite.config.js). Set this only when the
# API is deployed on a different origin than the frontend.
VITE_API_URL=
